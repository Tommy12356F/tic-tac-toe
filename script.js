const Gameboard = (function() {

    // private stuff here
    const board=["", "", "", "", "", "", "", "", ""]
    const placeMark = (index, marker) => {

    };
    const reset = ()=>{

    };
    const getBoard = ()=>{
        return board;

    };

    return {
        // public stuff here
        getBoard, placeMark, reset 
    };
    
    

})();
